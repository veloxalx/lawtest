/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com',"https://lawtest-delta.vercel.app/"],
    },
  };
  
  export default nextConfig;
  